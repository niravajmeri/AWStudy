<%= form_for(@subtask_question, html: {class: 'form-horizontal'}) do |f| %>
  <% if @subtask_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subtask_question.errors.count, "error") %> prohibited this subtask_question from being saved:</h2>

      <ul>
      <% @subtask_question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :subtask_id, class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.number_field :subtask_id, class: 'form-control' %>
    </div>
  </div>
  <div class="field form-group">
    <%= f.label :description, class: 'col-sm-2 control-label'   %>
    <div class="col-sm-10">
      <%= f.text_field :description, class: 'form-control' %>
    </div>
  </div>
  <div class="actions form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit 'Submit', class: 'btn btn-default' %>
    </div>
  </div>
<% end %>
