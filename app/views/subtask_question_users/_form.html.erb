<%= form_for(@subtask_question_user) do |f| %>
  <% if @subtask_question_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subtask_question_user.errors.count, "error") %> prohibited this subtask_question_user from being saved:</h2>

      <ul>
      <% @subtask_question_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :subtask_question_id %><br>
    <%= f.number_field :subtask_question_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
